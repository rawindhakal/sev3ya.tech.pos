DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/cakezake_pos?schema=public"
PORT=4000
CORS_ORIGINS="http://localhost:3000"

# Required in production — signs staff login tokens. If unset, the API
# refuses to start when NODE_ENV=production (see common/token.ts). Generate
# with: openssl rand -hex 32
AUTH_SECRET=""
